(var a (prompt "times:"))
(var text (prompt "ur text:"))
(if (!= (type {a}) "number") (var a 10))
(if (!= (type {text}) "nil") (var text "HELLO WORLD!!!!!!"))

(for (get a)
  (var r (random 11 99))
  (var g (random 11 99))
  (var b (random 11 99))
  (clear)
  (print (format "<font color='#{1}{2}{3}'><b>{4}</b></font>" (get r) (get g) (get b) (get text)))
  (sleep .1)
)